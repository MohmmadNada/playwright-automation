# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## Commands

```bash
# Setup (first time)
npm ci
npx playwright install --with-deps

# Run tests
npm test                                            # all tests, Chrome + Firefox
npm run test:ui                                     # UI tests only
npm run test:api                                    # API tests only
npm run test:headed                                 # headed mode (visible browser)
npx playwright test --grep TC_UI_001               # single test by ID
npx playwright test tests/ui/login.spec.ts         # single spec file
npx playwright show-report reports                 # open HTML report locally
```

---

## 1. Tech Stack

| Layer | Tool / Version |
|---|---|
| Test runner | Playwright (latest) |
| Language | TypeScript — strict mode |
| Browsers | Google Chrome, Mozilla Firefox |
| Test targets | SauceDemo (UI) · Simple Books API (API) |
| CI | GitHub Actions |
| Reports | Playwright HTML reporter → `reports/` |
| Node | LTS (see `package.json` `engines` field) |

### Base URLs
Each Playwright project declares its own `baseURL` — never hardcode full URLs in test files or page objects:
- UI projects (Chrome, Firefox): `https://www.saucedemo.com` — use `page.goto('/')`, `page.goto('/inventory.html')`, etc.
- API project: `https://simple-books-api.glitch.me` — the `request` context resolves paths automatically.

The only exception is `global-setup.ts`, which runs outside project context and must use the full URL directly.

---

## 2. Folder Structure

```
playwright-automation/
├── src/
│   ├── pages/          # Page Object classes — one file per page
│   ├── api/            # API client class — wraps all HTTP calls
│   ├── fixtures/       # Custom Playwright fixture extensions
│   ├── utils/          # Pure utility/helper functions
│   └── data/           # Test data JSON files (data-driven input)
├── tests/
│   ├── ui/             # UI spec files (import from ui.fixtures.ts)
│   └── api/            # API spec files (import from api.fixtures.ts)
├── reports/            # Generated HTML reports (gitignored)
├── global-setup.ts     # Suite-level setup: logging + auth state
├── global-teardown.ts  # Suite-level teardown: logging
├── storageState.json   # Saved browser session (gitignored, generated at runtime)
├── playwright.config.ts
├── tsconfig.json
└── .github/workflows/playwright.yml
```

**How a test call flows:**
```
test file
  → imports test from src/fixtures/*.ts     ← custom fixture, not raw Playwright
  → fixture injects src/pages/* or src/api/*
  → page/client reads src/data/*.json for inputs
  → dynamic values generated by src/utils/helpers.ts
```

---

## 3. Design Patterns

### Page Object Model (POM)
Each page in `src/pages/` maps to one application page. Locators and interactions are encapsulated inside the class — test files never interact with the DOM directly.

### API Client
`src/api/BooksApiClient.ts` owns all HTTP calls to Simple Books API. It exposes typed async methods and returns typed results. Test files never call `request.get/post/patch/delete` directly.

### Custom Fixtures (Dependency Injection)
`src/fixtures/ui.fixtures.ts` and `api.fixtures.ts` extend Playwright's base `test` with pre-instantiated page objects and the API client. This is the DI layer — tests declare what they need in the function signature, fixtures provide it.

### Global Authentication (Storage State)
`global-setup.ts` performs a single headless SauceDemo login before the suite runs and saves the session to `storageState.json`. Tests that need a logged-in state (TC_UI_004) consume this file via their fixture — they never re-login through the UI.

### Data-Driven Testing
All external test inputs live in `src/data/*.json`. Parametrised scenarios (e.g. invalid login) use `test.each()` over the JSON array — never duplicated as separate test blocks.

---

## 4. Coding Standards

### TypeScript
- Strict mode is enforced via `tsconfig.json` — `no any`, `strictNullChecks`, `noImplicitAny`
- Path alias `@src/*` → `src/*` — use it for all internal imports
- Prefer `interface` for object shapes, `type` for unions/aliases
- All async functions must be `async/await` — no `.then()` chains
- Return types must be explicit on all public methods

### SOLID Principles Applied
| Principle | How it applies here |
|---|---|
| **S** — Single Responsibility | Each page class handles exactly one page. `helpers.ts` only generates data. Fixtures only wire dependencies. |
| **O** — Open/Closed | Extend fixtures by composing them (`ui.fixtures.ts` merges into `api.fixtures.ts` if needed) — never modify the base `test` import directly. |
| **L** — Liskov Substitution | Page classes are interchangeable within their fixture — a fixture typed to `LoginPage` always delivers a complete `LoginPage`. |
| **I** — Interface Segregation | Fixtures expose only what each test suite needs — UI fixtures don't expose API client, API fixtures don't expose page objects. |
| **D** — Dependency Inversion | Test files depend on fixture abstractions, not on `new LoginPage(page)` concrete instantiation. |

### General
- No comments unless the WHY is non-obvious (a hidden constraint, a workaround, a subtle invariant)
- Add JSDoc to functions and methods only when the behaviour would surprise a reader. JSDoc is not documentation for documentation's sake — it's a warning sign that a method has a non-obvious contract. Ask: "Would a reader be surprised by this behaviour if they only read the name and types?" If no, skip it. Common cases that warrant JSDoc: uniqueness guarantees, inclusive/exclusive bounds, floating-point rounding, side effects not obvious from the name
- No dead code, no `console.log` left in committed files
- DRY: if a sequence repeats across two tests, it belongs in a page method or a fixture

---

## 5. Test Automation Best Practices

- **One concern per test** — each test case asserts one behaviour; setup steps are not assertions
- **Use Playwright auto-waits** — never add `waitForTimeout`. Use `waitForSelector`, `waitForURL`, or rely on locator auto-waiting
- **Assertion messages** — always pass a message to `expect()` on non-obvious assertions so failures are self-explanatory
- **Test IDs in names** — test titles must start with the TC ID followed by a behaviour-first description: `TC_UI_001 — should redirect to inventory page after valid login`. The ID provides traceability and `--grep` targeting; the description makes failing tests self-documenting without needing a lookup table. Never use vague labels like `TC_UI_001 — Valid Login`
- **API test isolation** — each API test must set up its own state in `beforeEach` (authenticate + create order). Never share `token` or `orderId` across tests via describe-scope variables without `beforeEach` — shared state causes cascading failures and hides the true failure point
- **Describe block naming** — top-level `describe` names the feature or resource under test (e.g. `'Orders API'`, `'Login'`). Nested `describe` blocks use `'given <precondition>'` to describe the context explicitly (e.g. `'given an existing order'`). This makes the full test path self-documenting: `Orders API › given an existing order › TC_API_002 — should retrieve…`
- **Dynamic data for API** — use `randomEmail()` from helpers for API client registration to avoid state conflicts between runs
- **No login repetition** — UI tests that need an authenticated state use `storageState.json` via fixture; only the global setup performs the actual login

---

## 6. Anti-Patterns to Avoid

| Anti-Pattern | Why / What to do instead |
|---|---|
| Any `page.*` call in a spec file | Breaks POM. Spec files must only call page object methods and make assertions — all browser interaction (`goto`, `locator`, `fill`, `click`, etc.) belongs in the page class |
| Data manipulation logic in spec files | Sorting, filtering, and slicing fetched data is a "how" — it belongs in a page method (e.g. `getTopProductsByPrice(n)`). Spec files only express intent: fetch, act, assert |
| Raw `request.*` calls in tests | Breaks API client pattern. Use `BooksApiClient` methods |
| `page.waitForTimeout()` / `sleep()` | Causes flakiness. Use Playwright's built-in auto-waits or explicit `waitFor*` |
| `any` type in TypeScript | Defeats strict typing. Define an interface or type alias |
| Hardcoded credentials in test files | Use `src/data/ui-test-data.json` and read at runtime |
| Magic numbers in spec files | Hard-coded values that define test scope (counts, thresholds) belong in the relevant data JSON file. Spec files read from data, they don't define it |
| Importing `test` from `@playwright/test` in spec files | Import from the relevant fixture file instead so page objects are injected |
| Separate test blocks per scenario variant | Use `test.each()` with the JSON data array |
| Batching multiple phases into one commit | One commit per phase — keep git history meaningful |

---

## 7. Component Rules

### Page Objects (`src/pages/`)
- Constructor signature: `constructor(private readonly page: Page) {}`
- **Spec files must never call Playwright APIs directly** — no `page.goto()`, `page.locator()`, `page.fill()`, `page.click()`, or any `page.*` call belongs in a spec file. Every browser interaction is encapsulated in a page object method. Spec files only orchestrate calls to page methods and make assertions
- Each page class must expose a `goto()` method that navigates to its own relative URL path — this is one application of the rule above
- All locators must be defined as `private` getters at the top of the class — never call `this.page.locator()` inline inside a method body
- Methods return `void` for actions, or a specific typed value for queries (e.g. `string[]`, `number`)
- No assertions inside page methods — assertions belong in the test
- No direct JSON imports — page classes receive data as method parameters

### Fixtures (`src/fixtures/`)
- `ui.fixtures.ts` extends `test` — provides `loginPage`, `productsPage`, `cartPage`, `checkoutPage`
- `api.fixtures.ts` extends `test` — provides `booksApi` (a `BooksApiClient` instance using the shared `request` context)
- Fixtures instantiate page objects using `async ({ page }, use) => { await use(new XxxPage(page)) }`
- Never duplicate fixture logic — if two fixtures share setup, extract a base fixture

### API Client (`src/api/BooksApiClient.ts`)
- Constructor takes Playwright's `APIRequestContext`
- All methods are `async` and return typed interfaces — never expose raw `APIResponse`
- Status code assertions (`expect(response.status()).toBe(...)`) live in the **test**, not in the client
- The client only sends requests and parses responses — it does not assert

---

## 8. Helper / Utility Rules (`src/utils/helpers.ts`)

- All functions are **pure** — no side effects, no state, no imports from Playwright
- Responsibilities: generate random strings, emails, and numbers; and provide pure calculation utilities (e.g. `sumPrices`) that keep computation logic out of spec files
- `randomEmail()` must include a timestamp or UUID suffix to guarantee uniqueness across parallel or back-to-back runs
- Domain types (e.g. `ProductWithPrice`) may be imported when needed for typed utility functions
- No browser interaction or test orchestration — helpers are pure functions only
- Consumed by `global-setup.ts` (for dynamic API registration email), test files, and page objects

---

## CI/CD

`.github/workflows/playwright.yml` triggers on:
- Push to `main`
- Daily schedule: `cron: '0 0 * * *'` — 00:00 UTC = 3:00 AM Amman (UTC+3)

Artifacts: `reports/` is uploaded after every run regardless of pass/fail.

## Git Conventions

- Branch per phase: `feat/project-setup`, `feat/ui-layer`, `feat/api-layer`, `feat/api-tests`, `feat/ci-cd`, `docs/readme`
- Conventional commits: `feat:`, `ci:`, `docs:`
- One commit per phase minimum

## Local Tracker

`TRACKER.md` (gitignored) is the single source of truth for progress and per-phase implementation details — checklists, file targets, data shapes, and commit messages. Read it before starting any phase.
